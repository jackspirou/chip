package main

func main() {
    xs := []int{3, 1, 4, 1, 5}
    print(xs)
    print(len(xs))
    print(sum(xs))
    xs[0] = 100
    print(xs[0])
}

func sum(xs []int) int {
    total := 0
    i := 0
    for i < len(xs) {
        total = total + xs[i]
        i = i + 1
    }
    return total
}
